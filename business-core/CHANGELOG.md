# Changelog

## 0.0.8-test
- Housekeeping release used to exercise skill resume flow (state B).
- Bump `VERSION_NAME` in `gradle.properties`.

- Minor housekeeping release. No functional changes.
- Bump `VERSION_NAME` in `gradle.properties`.

## 0.0.6-test
- Previous test release.
